apiVersion: v1
kind: Service
metadata:
  name: {{ include "grove-kc.fullname" . }}
  labels:
    {{- include "grove-kc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "grove-kc.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
